rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Public read of the most recent PDF. Only an authenticated admin can
    // upload a replacement.
    match /asignaciones/{file=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
